# **Лабораторна робота №1**

**Тема:** Створення веб-сторінки

**Мета:** Закріпити практичні навички створення HTML-документа: правильна структура сторінки, використання семантичних елементів HTML5, робота з текстовим контентом, списками, посиланнями, зображеннями та таблицями.

**Обладнання:** IBM-сумісні ПК

**Програмне забезпечення:** ОС Windows 10/11, macOS або Linux; веб-браузер (Chrome, Firefox, Edge); текстовий редактор (VS Code, Sublime Text або інший)

## **Контрольні запитання**

1. Яку роль відіграє `<!DOCTYPE html>` і що станеться, якщо його не вказати?
2. Яка різниця між `<div>` та семантичними елементами (`<header>`, `<main>`, `<section>`, `<footer>`)? Чому семантика важлива?
3. Для чого потрібен атрибут `alt` у елемента `<img>`? Що станеться, якщо його не вказати?
4. Яка різниця між `<ol>` та `<ul>`? Наведіть приклади, коли доцільно використовувати кожен з них.
5. З яких структурних частин складається HTML-таблиця? Поясніть призначення `<thead>`, `<tbody>` та `<th>`.

## **Теоретичні відомості**

### **Структура HTML-документа**

Кожен HTML-документ має обов'язкову базову структуру. `<!DOCTYPE html>` повідомляє браузеру, що документ використовує стандарт HTML5. Елемент `<html>` є кореневим, `<head>` містить метадані (кодування, заголовок вкладки, підключення стилів), а `<body>` — видимий контент сторінки.

```html
<!DOCTYPE html>
<html lang="uk">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Назва сторінки</title>
  </head>
  <body>
    <!-- Видимий контент -->
  </body>
</html>
```

### **Семантичні елементи HTML5**

Семантичні елементи описують своє призначення у структурі документа, на відміну від універсального `<div>`. Їх використання покращує доступність сторінки для screen readers, індексацію пошуковими системами (SEO) та читабельність коду для розробників.

| Елемент     | Призначення                                     |
| ----------- | ----------------------------------------------- |
| `<header>`  | Шапка сторінки або секції (логотип, навігація)  |
| `<nav>`     | Блок навігаційних посилань                      |
| `<main>`    | Головний унікальний контент (один на сторінку)  |
| `<section>` | Тематичне групування контенту                   |
| `<footer>`  | Підвал сторінки або секції (копірайт, контакти) |
| `<article>` | Самостійна одиниця контенту (стаття, пост)      |
| `<aside>`   | Додатковий контент (бічна панель)               |

> **Примітка:** У цій лабораторній обов'язковими є `<header>`, `<nav>`, `<main>`, `<section>` та `<footer>`. Елементи `<article>` та `<aside>` наведені для ознайомлення — ви можете використати їх за бажанням.

### **Текстові елементи**

HTML надає набір елементів для структурування текстового контенту:

- **Заголовки** `<h1>`–`<h6>` — утворюють ієрархію від найважливішого до найменш важливого. На сторінці має бути лише один `<h1>`, а рівні заголовків не слід пропускати (наприклад, після `<h2>` має йти `<h3>`, а не `<h4>`).
- **Параграфи** `<p>` — основний елемент для блоків тексту.
- **Виділення** — `<strong>` для семантичного виділення важливого тексту (візуально — жирний), `<em>` для смислового наголосу (візуально — курсив).

### **Списки**

- `<ul>` — ненумерований (маркований) список, коли порядок елементів не має значення.
- `<ol>` — нумерований список, коли важлива послідовність (інструкції, рейтинги).
- `<li>` — окремий пункт списку (використовується всередині `<ul>` або `<ol>`).

### **Посилання та зображення**

```html
<!-- Посилання -->
<a href="https://example.com">Зовнішнє посилання</a>
<a href="about.html">Внутрішнє посилання</a>
<a href="#contacts">Якірне посилання</a>

<!-- Зображення -->
<img src="photo.jpg" alt="Опис зображення" width="600" height="400" />
```

Атрибут `alt` у `<img>` є обов'язковим — він забезпечує доступність для людей з вадами зору та відображається, якщо зображення не завантажилося.

### **Таблиці**

HTML-таблиці призначені для відображення структурованих табличних даних. Основні елементи:

- `<table>` — контейнер таблиці
- `<thead>` — секція заголовків стовпців
- `<tbody>` — секція основного вмісту
- `<tr>` — рядок таблиці
- `<th>` — комірка-заголовок
- `<td>` — комірка з даними

```html
<table>
  <thead>
    <tr>
      <th>Назва</th>
      <th>Значення</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Елемент</td>
      <td>Опис</td>
    </tr>
  </tbody>
</table>
```

## **Хід роботи**

У цій лабораторній роботі ви створите HTML-сторінку на **вільну тему** (персональна візитка, сторінка про улюблений фільм, технологію, місто, хобі тощо). Тема — на ваш вибір, але сторінка повинна містити всі обов'язкові елементи, перелічені в Етапі 2.

### **Етап 1: Підготовка**

1.1. Створіть папку для лабораторної роботи, наприклад `lab1`.

1.2. Всередині папки створіть файл `index.html`.

1.3. Створіть підпапку `images` для зображень, які будуть використані на сторінці.

1.4. Підготуйте 2–3 зображення, що відповідають обраній вами темі, та збережіть їх у папку `images`. Можна використати безкоштовні зображення з сервісів [Unsplash](https://unsplash.com) або [Pexels](https://www.pexels.com).

**Очікувана структура:**

```
lab1/
├── images/
│   ├── photo1.jpg
│   └── photo2.jpg
└── index.html
```

### **Етап 2: Створення сторінки**

Створіть HTML-сторінку на обрану вами тему, яка відповідає **усім вимогам**, переліченим нижче.

### **Обов'язкові елементи**

#### 📄 Структура документа

- `<!DOCTYPE html>` на початку файлу
- `<html lang="uk">` — кореневий елемент з атрибутом мови
- `<head>` з `<meta charset="UTF-8">`, `<meta name="viewport" ...>` та `<title>`
- `<body>` з усім видимим контентом

#### 🏷️ Семантичні елементи

- `<header>` — шапка сторінки (заголовок сайту та/або навігація)
- `<nav>` — навігаційний блок з посиланнями (мінімум 3 посилання)
- `<main>` — основний контент сторінки
- `<section>` — мінімум 2 тематичні секції з заголовками
- `<footer>` — підвал сторінки (копірайт, контакти або інше)

#### 📝 Текстовий контент

- `<h1>` — один головний заголовок сторінки
- `<h2>` та/або `<h3>` — підзаголовки секцій (мінімум 2)
- `<p>` — параграфи тексту (мінімум 3)
- `<strong>` та `<em>` — виділення важливого тексту та акценти (хоча б по одному)

#### 📋 Списки

- `<ul>` — один ненумерований список (мінімум 3 пункти)
- `<ol>` — один нумерований список (мінімум 3 пункти)

#### 🔗 Посилання

- `<a>` — мінімум 3 посилання (зовнішні, внутрішні або якірні)
- Хоча б одне якірне посилання (`href="#id-секції"`) для навігації по сторінці

#### 🖼️ Зображення

- `<img>` — мінімум 2 зображення з локальної папки `images/`
- Кожне зображення має атрибути `src`, `alt`, `width` та `height`

#### 📊 Таблиця

- `<table>` з `<thead>` і `<tbody>`
- Мінімум 3 стовпці та 3 рядки даних
- `<th>` для заголовків стовпців

### **Етап 3: Перевірка**

3.1. Відкрийте файл `index.html` у браузері та переконайтесь, що сторінка відображається коректно.

3.2. Пройдіться по списку обов'язкових елементів та переконайтесь, що всі вони присутні на сторінці.

3.3. Перевірте валідність HTML-коду за допомогою онлайн-валідатора: https://validator.w3.org/#validate_by_input (вставте код у поле та натисніть "Check").

3.4. Відкрийте DevTools (F12) → вкладка **Elements** та переконайтесь, що структура DOM-дерева відповідає вашій розмітці.

**Результат:** HTML-сторінка на вільну тему, що містить усі обов'язкові елементи та проходить валідацію.

## **Зміст звіту**

1. **Титульна сторінка** з темою, метою роботи, обладнанням, програмним забезпеченням

2. **Відповіді на контрольні питання** (коротко, по суті)

3. **Виконання роботи:**

   3.1. Скріншот структури папки проєкту (папка `lab1` з файлами)

   3.2. Скріншот коду `index.html` (або декілька скріншотів, якщо код не вміщується на одному екрані)

   3.3. Скріншот сторінки у браузері (загальний вигляд)

   3.4. Скріншот результату валідації HTML-коду (https://validator.w3.org)

4. **Висновки** (що навчились робити, які навички отримали, з якими труднощами зіткнулись)

## **Типові помилки та їх вирішення**

| Проблема                                              | Рішення                                                                               |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------- |
| Зображення не відображається                          | Перевірте правильність шляху в `src` — файл має бути в папці `images/`                |
| Кирилиця відображається некоректно (кракозябри)       | Переконайтесь, що у `<head>` є `<meta charset="UTF-8">` та файл збережено в UTF-8     |
| Якірне посилання не працює                            | Перевірте, що значення `href="#id"` збігається з атрибутом `id` цільового елемента    |
| Валідатор показує помилку "Element X not allowed"     | Перевірте вкладеність елементів — наприклад, `<p>` не може містити `<div>` або список |
| Зображення занадто велике і виходить за межі сторінки | Додайте атрибути `width` та `height`, або використайте CSS `max-width: 100%`          |

## **Додаткові матеріали**

1. HTML Tutorial (W3Schools, українською) — https://w3schoolsua.github.io/html/index.html

2. HTML-валідатор W3C — https://validator.w3.org

3. Unsplash (безкоштовні зображення) — https://unsplash.com

4. Pexels (безкоштовні зображення) — https://www.pexels.com
